{
  "name": "dojo-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev && npx json-server --watch data/db.json --port 8000",
    "build": "next build",
    "start": "next start && json-server --watch data/db.json --port 8000",
    "push": "git add . && git commit -m 'commit' && git push origin main"

  },
  "dependencies": {
    "next": "10.0.6",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
